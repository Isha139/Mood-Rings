# Mood-Rings
A fun little project that simulates a digital mood ring. Every time the page loads, the center stone changes to a random color, representing a different “mood.” Built using HTML, CSS, and JavaScript to practice DOM updates and styling transitions.
